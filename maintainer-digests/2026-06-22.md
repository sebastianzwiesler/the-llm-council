# Weekly Maintainer Digest — 2026-06-22 (run 4)

Read-only maintenance pass with one safe, branch-only build. Nothing merged,
released, tagged, pushed to protected branches, or posted to public issues.

## Executive Summary

- **Built:** `engine/degradation.py` + `engine/health.py` unit-test coverage — 57 new
  tests, full suite **436 passed**, on `claude/feature-engine-tests-2026-06-22` (branch only, no PR).
- **Planned (not built — out of safe build scope):** the #1 item is merging the three
  waiting Dependabot security PRs (#47/#48/#49); that is a human merge action, not a build.
- **Nothing else this week — because** the top-RICE item is a prohibited merge and the
  remaining feature candidates are either flagged or multi-run architectural work.

---

## 1. Repo Health

**HEALTH: watch** — CI is green and `main` is clean, but 3 open security advisories have
unmerged fixes, 2 PRs are >90 days stale with merge conflicts, and the provider/CLI layer
has thin test coverage.

| Area | Status |
|------|--------|
| CI on `main` | ✅ Green — `test (3.10/3.11/3.12)` all pass; PyPI publish OK; TestPyPI correctly skipped on non-tag |
| Release | v0.7.18 (2026-06-10); `main` == v0.7.18, 0 commits ahead. Healthy cadence. |
| Open PRs | 9 total — 5 routine Dependabot; **3 security fixes unmerged 21d**; 2 very stale (>90d) |
| Security advisories (open) | **#10 idna** (med), **#7 pytest** (med), **#6 Pygments** (low) — fixes sit in PRs #48/#49/#47, mergeable, unmerged 21d |
| Stale PRs | **#42** (46d, CONFLICTING — Codex isolated-home/turn.failed); **#36** (96d draft, task-adaptive-protocol); **#24/#25** (114d, stale actions bumps) |
| Test drift | ~40% file-level coverage; provider adapters + CLI layer + `engine/degradation.py`/`engine/health.py` were the biggest gaps |
| Doc drift | None material — deprecated aliases documented; capability-augmented spec is forward-looking, not drift |
| mypy | **Now clean** (`Success: no issues found in 43 source files`) — the "~78 errors" in CLAUDE.md Known Issues is stale and should be removed |

**Maintainer action items (for a human — outside this bot's safe scope):**
1. **Merge Dependabot PRs #47/#48/#49** — clears all 3 open security advisories (idna, pytest, pygments). Mergeable, no conflicts, waiting 21 days.
2. Rebase or close **PR #42** (46d, conflicts) and **PR #36** (96d draft).
3. Close stale **PRs #24/#25** (114d, superseded actions bumps).
4. Delete merged stale `claude/` branches (see §4).

---

## 2. Feature Candidates (RICE-ranked)

| Rank | Feature | Source | R | I | C | E | Final | Flags | Buildable in 1 run? |
|------|---------|--------|---|---|---|---|-------|-------|----------------------|
| 1 | Merge Dependabot security PRs (#47/#48/#49) | open PRs | 4 | 3 | 5 | 1 | 60.0 | — | ❌ merge action (prohibited) |
| 2 | Live cost pricing from OpenRouter API | gap + competitor | 4 | 4 | 4 | 2 | 32.0 | new-deps | ⚠️ adds network dep |
| 3 | Rebase + merge PR #42 (Codex isolated-home/turn.failed) | open PRs | 3 | 4 | 4 | 2 | 24.0 | — | ❌ merge action |
| **4** | **engine/degradation + engine/health unit tests** | **gap** | **3** | **3** | **5** | **2** | **22.5** | **—** | **✅ BUILT** |
| 5 | Mode-specific eval dataset (P2 seed) | roadmap | 3 | 3 | 3 | 2 | 13.5 | — | ✅ |
| 6 | Stale CI actions Dependabot PRs (#24/#25/#52/#53) | open PRs | 2 | 1 | 5 | 1 | 10.0 | — | ❌ merge action |
| 7 | Capability pack execution (P1) | roadmap + competitor | 2 | 5 | 2 | 4 | 5.0 | multi-run, speculative | ❌ |
| 8 | Streaming output to CLI | competitor | 2 | 4 | 2 | 4 | 4.0 | multi-run, speculative | ❌ |

`TOP: Merge Dependabot security PRs (#47/#48/#49) — Final 60.0`

---

## 3. Gate Decision & What Was Built

**Gate recalibration (run 4 — "recalibrate after 3 runs"):** The scout scores "Final" in
RICE magnitude (10–60), not the 1–10 the original `>=8` threshold assumed, so a literal
threshold is meaningless. More importantly, the top-RICE item has been an *un-buildable
merge/architectural item* in every run so far (runs 1–3 all built nothing). Recalibrated
gate, applied going forward:

> Build the **highest-Final candidate that is (a) a buildable feature** — not a
> merge/release/rebase of an existing PR (those are maintainer recommendations),
> **(b) unflagged** (no multi-run/speculative/new-deps), and **(c) self-contained and
> verifiable in one run.**

Under that gate the prohibited merges (#1, #3, #6) and the `new-deps`-flagged live-pricing
item (#2) drop out, and the winner is **#4 — engine test coverage** (Confidence 5/5, no
flags, tests-only, zero new deps, closes the health scan's #1 structural gap).

**Built:** `claude/feature-engine-tests-2026-06-22`
- Commit `92c4fed` — `test(engine): add dedicated unit tests for degradation and health`
- Added `tests/test_degradation.py` (26 tests) + `tests/test_health.py` (31 tests) = **57 tests**
- `pytest` new files: `57 passed in 0.15s`; **full suite: `436 passed`**
- `ruff check` / `ruff format --check`: clean
- Zero production code changes; no new dependencies
- **Branch only — not pushed, no PR, not merged.** For Sherif's review.

**Builder sanity note (honest):** the modules were not *entirely* untested —
`tests/test_engine.py` already smoke-tests happy paths. The new files deliberately target
genuinely under-covered branches (degradation: MODEL_UNAVAILABLE±fallback, UNKNOWN-error
single-retry, abort-below-minimum vs `abort_on_all_failures=False`, backoff growth + 10s
cap, per-(provider,phase) retry tracking, `reset()`; health: AUTH/BILLING→DOWN vs
transient→DEGRADED, `should_skip_provider` matrix, cache TTL/clear, mixed-status
aggregation). One unreachable `gather` BaseException branch was intentionally skipped rather
than tested vacuously.

---

## 4. Stale `claude/` Branch Cleanup List

Merged work, safe to delete (all 21 days old, work already in `main` via the noted PRs):

- `claude/weekly-maintainer-2026-06-01`
- `claude/fix-ci-ruff-format-2026-06-01` (merged #44)
- `claude/security-deps-2026-06-01` (merged #45)
- `claude/gate-publish-on-ci-2026-06-01` (merged #46)
- `claude/feature-markdown-output-2026-06-01` (merged #43)

At the 14-day threshold (review before deleting):
- `claude/weekly-maintainer-2026-06-08`

Active / keep:
- `claude/weekly-maintainer-2026-06-15` (last run's digest)
- `claude/weekly-maintainer-2026-06-22` (this digest)
- `claude/feature-engine-tests-2026-06-22` (this week's build, pending review)

---

## 5. Implementation Plan for the Top Item (#1, not built here)

**Merge Dependabot security PRs #47 (Pygments), #48 (idna), #49 (pytest).** All three are
patch-level dependency bumps, fully mergeable with no conflicts, each resolving an open
GitHub security advisory, and they have sat unmerged for 21 days. Recommended action for a
human maintainer: review the three diffs (they are mechanical version bumps), confirm CI is
green on each, and merge all three — ideally in one sitting to clear the advisory queue.
This is intentionally **not** done by this bot because merging is a prohibited write action
under the maintainer safety rules; it is surfaced here as the single highest-value action
for the week.
